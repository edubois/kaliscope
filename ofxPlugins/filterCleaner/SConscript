Import( 'project', 'libs' )

project.createOfxPlugin(
        dirs = ['src'],
        libraries = [
            libs.terry,
            libs.tuttlePlugin,
            libs.boost,
            libs.boost_log,
            libs.openfxHack,
    ] )

